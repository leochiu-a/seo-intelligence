# Quick Task 260415-m0h: fix popover z-index covered by step node and remove redundant right node sidebar

## Task 1: Fix EditPopover z-index
- **File:** `src/components/UrlNode.tsx`
- **Action:** Import `useEffect` and `useReactFlow`. When `showPopover` changes, call `setNodes` to set `zIndex: 1000` on the open node (reset to `0` on close). This escapes the React Flow wrapper stacking context.

## Task 2: Remove ScoreSidebar
- **File:** `src/App.tsx`
- **Action:** Remove `ScoreSidebar` import and JSX element. Keep `scores`/`weakNodes` computations (still used for node enrichment).

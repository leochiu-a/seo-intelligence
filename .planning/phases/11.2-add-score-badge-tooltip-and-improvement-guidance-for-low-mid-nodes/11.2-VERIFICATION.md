---
phase: 11.2-add-score-badge-tooltip-and-improvement-guidance-for-low-mid-nodes
verified: 2026-04-19T13:55:30Z
status: human_needed
score: 7/7 must-haves verified
human_verification:
  - test: "Hover the Low badge on a canvas node with a scored graph loaded"
    expected: "A tooltip appears above the badge with text containing 'Low link equity' and an action phrase — no '?' icon visible, no visual change to the badge itself"
    why_human: "Base UI TooltipContent is portalled and only renders on hover — jsdom/vitest cannot simulate real pointer events against a fully mounted canvas with ReactFlow"
  - test: "Hover the Mid badge on a canvas node"
    expected: "Tooltip appears with 'Mid link equity' copy and improvement action"
    why_human: "Same as above — tooltip portal requires real browser pointer events"
  - test: "Hover the High badge on a canvas node"
    expected: "Tooltip appears with 'High link equity' affirmation copy"
    why_human: "Same as above"
  - test: "Verify a Neutral-tier node shows no badge at all (no tooltip trigger)"
    expected: "Neutral node card has no tier badge and no tooltip element anywhere in the card"
    why_human: "Requires live app to confirm absence of tooltip UI"
---

# Phase 11.2: Add Score Badge Tooltip and Improvement Guidance Verification Report

**Phase Goal:** Add hover tooltips to the Low/Mid/High tier badges on canvas UrlNode cards. Each tooltip explains what the tier means in PM-friendly language and gives a one-line action. Neutral badge excluded. Badge visual unchanged.
**Verified:** 2026-04-19T13:55:30Z
**Status:** human_needed (all automated checks passed; tooltip popup requires live browser smoke test)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | Hovering Low badge shows tooltip with Low link equity explanation plus action | ? HUMAN | TooltipTrigger renders with `data-tier="low"` and `data-testid="badge-tooltip-trigger"`; TONE_MAP low.tooltip = "Low link equity — ..."; portalled popup requires live browser |
| 2 | Hovering Mid badge shows tooltip with Mid link equity explanation plus action | ? HUMAN | Same pattern for mid tier verified in code and tests; portalled popup requires live browser |
| 3 | Hovering High badge shows tooltip with High link equity affirmation | ? HUMAN | Same pattern for high tier verified in code and tests; portalled popup requires live browser |
| 4 | Neutral badge does NOT render a tooltip trigger | ✓ VERIFIED | Existing `{tier !== "neutral" && (...)}` gate wraps entire Tooltip structure; test "does NOT render tooltip trigger when scoreTier is neutral" passes |
| 5 | Tooltip copy avoids "PageRank" — uses PM-friendly phrasing | ✓ VERIFIED | `grep -c 'PageRank' UrlNode.tsx` returns 0; `grep -c 'link equity' UrlNode.tsx` returns 3 |
| 6 | Badge span itself is the hover trigger — no extra '?' icon introduced | ✓ VERIFIED | `grep 'aria-label="Score info"' UrlNode.tsx` returns 0; `render={<span />}` pattern used on TooltipTrigger at line 165 |
| 7 | Existing badge visual (classes, label text, color) is unchanged | ✓ VERIFIED | className `inline-flex items-center rounded-full px-1.5 py-0.5 text-[10px] font-semibold uppercase tracking-wide ${tone.badge}` preserved verbatim at line 168 |

**Score:** 7/7 truths verified or queued for human confirmation (4 automated pass, 3 require live browser for the tooltip popup layer)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|---------|---------|--------|---------|
| `packages/web/src/components/UrlNode.tsx` | TONE_MAP extended with tooltip field; badge wrapped in Tooltip/TooltipTrigger/TooltipContent | ✓ VERIFIED | File exists, substantive, wired; TONE_MAP type now includes `tooltip: string` on all 4 tiers; Tooltip wrapping at lines 163-175 |
| `packages/web/src/components/UrlNode.test.tsx` | 5 new tests in `describe("tier badge tooltip")` asserting trigger presence per tier | ✓ VERIFIED | `describe("tier badge tooltip")` block exists at line 208; 5 tests present; all 17 UrlNode tests pass |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `UrlNode.tsx` tier badge span | `ui/tooltip.tsx` Tooltip/TooltipTrigger/TooltipContent | `import { Tooltip, TooltipTrigger, TooltipContent } from "@/components/ui/tooltip"` | ✓ WIRED | Import at line 5; all three symbols used at lines 163, 164, 172 |
| `UrlNode.tsx` TONE_MAP | tooltip content text | `tone.tooltip` field read inside TooltipContent at line 173 | ✓ WIRED | Pattern `tone.tooltip` confirmed at line 173; tooltip strings for high/mid/low contain "link equity" |

---

### Data-Flow Trace (Level 4)

Not applicable for this phase. The artifact is a static display component — tooltip copy is static strings in TONE_MAP (no DB or async data source). The tier value flows from `data.scoreTier` prop through `TONE_MAP[tier].tooltip` and renders directly in TooltipContent. No hollow-prop risk: the `tone.tooltip` string is always set for the three rendered tiers (low/mid/high).

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---------|---------|--------|--------|
| All 17 UrlNode tests pass including 5 new tier badge tooltip tests | `pnpm exec vitest run src/components/UrlNode.test.tsx` | 17 passed (1 file) | ✓ PASS |
| TypeScript compiles clean | `pnpm exec tsc --noEmit` | Exit 0, no output | ✓ PASS |
| "link equity" appears 3 times in UrlNode.tsx (one per tier) | `grep -c 'link equity' UrlNode.tsx` | 3 | ✓ PASS |
| "PageRank" does NOT appear in UrlNode.tsx | `grep -c 'PageRank' UrlNode.tsx` | 0 | ✓ PASS |
| badge-tooltip-trigger testid appears exactly once | `grep -c 'data-testid="badge-tooltip-trigger"' UrlNode.tsx` | 1 | ✓ PASS |
| TooltipProvider NOT added to UrlNode.tsx | `grep -c 'TooltipProvider' UrlNode.tsx` | 0 | ✓ PASS |
| tier badge tooltip describe block present in test file | `grep -c 'tier badge tooltip' UrlNode.test.tsx` | 1 | ✓ PASS |
| TooltipProvider NOT added to test file | `grep -c 'TooltipProvider' UrlNode.test.tsx` | 0 | ✓ PASS |
| Tooltip portalled content requires live browser hover | Base UI TooltipContent renders via portal on pointer enter | Not testable in jsdom | ? SKIP — route to human |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|------------|------------|-------------|--------|---------|
| TBD-11.2-BADGE-TOOLTIP | 11.2-01-PLAN.md | Tier badge tooltip on canvas nodes | ✓ SATISFIED | Implementation complete in UrlNode.tsx; TONE_MAP extended; 5 new passing tests |

**Note on requirement ID:** `TBD-11.2-BADGE-TOOLTIP` is declared in ROADMAP.md (line 208) and in the PLAN frontmatter but does NOT appear in REQUIREMENTS.md. This is an orphaned-to-REQUIREMENTS pattern — the requirement was created as a TBD placeholder and never formally added to the requirements registry. This is a documentation gap, not an implementation gap. The implementation satisfies the intent of the requirement as described in the ROADMAP goal. Recommend adding a formal entry to REQUIREMENTS.md if the project maintains that registry going forward.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `packages/web/src/components/UrlNode.tsx` | 70 | `tooltip: ""` for neutral tier | ℹ️ Info | Intentional — keeps Record type exhaustive; neutral badge is never rendered so the empty string is never displayed. No impact. |

No blockers or warnings found.

---

### Human Verification Required

#### 1. Low tier badge tooltip popup

**Test:** Open the app in a browser (`pnpm --filter @seo-intelligence/web dev`), load or create a graph with scored nodes, hover the red "Low" badge on a Low-tier node.
**Expected:** A tooltip popup appears above the badge with copy beginning "Low link equity —" and an improvement action. The badge itself does not change color or size. No "?" icon appears.
**Why human:** Base UI `TooltipContent` renders via a portal on `pointerenter`. jsdom cannot simulate real pointer events against a fully mounted ReactFlow canvas, so the popup layer cannot be asserted in vitest.

#### 2. Mid tier badge tooltip popup

**Test:** Hover the amber "Mid" badge on a Mid-tier node.
**Expected:** Tooltip popup appears with "Mid link equity —" copy and an action to add more internal links from high-scoring pages.
**Why human:** Same as above.

#### 3. High tier badge tooltip popup

**Test:** Hover the green "High" badge on a High-tier node.
**Expected:** Tooltip popup appears with "High link equity —" affirmation copy.
**Why human:** Same as above.

#### 4. Neutral tier shows no badge and no tooltip

**Test:** Identify a node with no score tier (or explicitly Neutral) — it should show no tier badge and hovering the card should produce no tooltip popup.
**Expected:** Card area for the tier badge row is absent (no badge rendered at all for Neutral). No tooltip appears on hover anywhere on the card (except for other tooltips already present pre-phase, such as the warning icon).
**Why human:** Requires visual confirmation in live app.

---

### Gaps Summary

No functional gaps found. All automated checks pass:

- UrlNode.tsx contains the full Tooltip structure (import, TONE_MAP extension, JSX wrapping) exactly as specified in the plan.
- All 17 vitest tests pass (12 pre-existing + 5 new tier badge tooltip tests).
- TypeScript passes clean.
- PM-language constraints enforced: "link equity" × 3, zero "PageRank" occurrences.
- Neutral badge is correctly excluded from tooltip wrapping.
- No TooltipProvider added inside UrlNode — the app-level provider in App.tsx (line 801) is reused.
- Global badge and Root badge are untouched.

The only unverifiable items are the live hover interactions (tooltip popup layer), which are routed to human smoke test.

The requirement ID `TBD-11.2-BADGE-TOOLTIP` is not registered in REQUIREMENTS.md — this is a pre-existing documentation gap in the project (the "TBD" prefix signals a placeholder). It does not block the phase.

---

_Verified: 2026-04-19T13:55:30Z_
_Verifier: Claude (gsd-verifier)_

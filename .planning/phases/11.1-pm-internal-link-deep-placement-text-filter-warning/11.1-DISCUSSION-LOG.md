# Phase 11.1: PM Health Check Panel - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-18
**Phase:** 11.1-pm-internal-link-deep-placement-text-filter-warning
**Areas discussed:** Panel entry point, Metric definitions, Filter UX, Layout / density

---

## Panel Entry Point

| Option | Description | Selected |
|--------|-------------|----------|
| New tab alongside canvas | Second tab in main view: Canvas / Health Check, full-width panel | |
| Drawer / panel overlay | Toolbar button opens a right-side drawer overlaying the canvas | |
| Replaces ScoreSidebar when active | Toggle switches right sidebar between Score and Health views | ✓ |

**User's choice:** Replaces ScoreSidebar when active

---

## Sidebar Toggle Mechanism

| Option | Description | Selected |
|--------|-------------|----------|
| Toggle tabs at top of sidebar | Two small tabs [Score] [Health] at sidebar top | ✓ |
| Button in the toolbar | Health Check button in the toolbar | |
| Icon button in sidebar header | Small icon in sidebar title area | |

**User's choice:** Toggle tabs at top of sidebar (Recommended)

---

## Metric: Placement

| Option | Description | Selected |
|--------|-------------|----------|
| Global nodes with no placements | Global node with 0 placements = warning | |
| Nodes not targeted by any placement | Under-connected receiving-end check | |
| You decide | Defer to Claude | |
| Other (user input) | Placement is not a direct health metric — it feeds the SEO score as a parameter | ✓ |

**User's choice:** Placement 不是直接影響 Health，它是 PageRank 計算的參數，已反映在分數中。不作為獨立健康指標。

---

## Active Metric Dimensions

| Option | Description | Selected |
|--------|-------------|----------|
| 3 metrics: Links + Depth + Tags | Outbound >150, depth >3, no cluster tags | ✓ |
| 2 metrics: Links + Depth only | Tags are optional metadata, not a standard | |
| Re-define the 4 metrics | User describes custom dimensions | |

**User's choice:** 3 metrics: Links + Depth + Tags (Recommended)

---

## Tags Warning Condition

| Option | Description | Selected |
|--------|-------------|----------|
| Node has no cluster tags assigned | Empty tags array = warning | ✓ |
| You decide | Defer to Claude | |

**User's choice:** Node has no cluster tags assigned (Recommended)

---

## Filter UX

| Option | Description | Selected |
|--------|-------------|----------|
| "Show warnings only" toggle | Single checkbox, hides all-OK rows | ✓ |
| Per-metric filter checkboxes | Separate checkboxes per dimension | |
| Both: global toggle + per-metric | Most flexible | |

**User's choice:** "Show warnings only" toggle (Recommended)

---

## Layout Style

| Option | Description | Selected |
|--------|-------------|----------|
| Compact list rows with icon badges | URL + 3 icon badges (red=warn, muted=ok) | ✓ |
| Mini table with columns | Page / Links / Depth / Tags columns | |

**User's choice:** Compact list rows with icon badges (Recommended)

---

## Click Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Selects + focuses node on canvas | Same as Score sidebar click | |
| Nothing (read-only panel) | Purely informational | ✓ |

**User's choice:** Nothing (read-only panel)

---

## Claude's Discretion

- Exact icon choices for each badge (lucide-react vocabulary)
- Tab styling (pills vs underline)
- Summary line at top ("3/12 pages have warnings")
- Pure helper `getHealthStatus()` for TDD boundary
- Badge layout within each row

## Deferred Ideas

- Per-metric filter checkboxes (can extend later)
- Canvas click-through from health rows (can add if PM requests)
- Placement as direct health metric (confirmed: not needed)

---
status: complete
phase: 11.1-pm-internal-link-deep-placement-text-filter-warning
source: [11.1-01-SUMMARY.md, 11.1-02-SUMMARY.md]
started: 2026-04-18T14:00:00.000Z
updated: 2026-04-18T14:10:00.000Z
---

## Current Test

[testing complete]

## Tests

### 1. Score/Health tab visible
expected: Open the app. The right sidebar shows two tab buttons labeled "Score" and "Health" at the top. The active tab (Score by default) has a blue underline indicator beneath it.
result: pass

### 2. Health tab switch
expected: Clicking "Health" switches the sidebar to show the Health panel (no score list). Clicking "Score" switches back to the original score list. The blue underline follows the active tab.
result: pass

### 3. Health summary line
expected: In the Health tab, the header shows a summary line like "X / N pages have warnings" reflecting the actual node counts.
result: pass

### 4. Warning badges on rows
expected: Each node row in the Health panel shows icon badges for Links (chain icon), Tags (tag icon). Nodes with a warning show the badge in amber/red; nodes that are ok show it in muted gray. The Depth badge (layers icon) also appears when a root node is set.
result: pass

### 5. Warnings-first sort
expected: Nodes with any warning appear at the top of the list. Within the warning group, rows are sorted alphabetically. Healthy nodes appear below, also alphabetically.
result: pass

### 6. Show warnings only toggle
expected: The "Show warnings only" checkbox is checked by default, showing only rows with warnings. Unchecking it reveals all nodes. Re-checking it hides healthy rows again.
result: pass

### 7. All pages healthy empty state
expected: When "Show warnings only" is checked and every node is healthy (no warnings), the list area shows "All pages are healthy" instead of an empty list.
result: pass

### 8. Depth badge hidden when no root set
expected: In a graph with no root node designated, switch to the Health tab. Only the Links and Tags badges appear per row — the Depth badge column is absent (no layers icon shown).
result: skipped
reason: Implementation uses a single TriangleAlert icon (not per-metric badges); premise doesn't apply to current UI

### 9. Score tab regression
expected: After switching to Health and back to Score, the score list renders correctly — PageRank scores, node rows, and all existing Score tab functionality work exactly as before.
result: pass

## Summary

total: 9
passed: 7
issues: 0
skipped: 1
pending: 0

## Gaps

[none yet]

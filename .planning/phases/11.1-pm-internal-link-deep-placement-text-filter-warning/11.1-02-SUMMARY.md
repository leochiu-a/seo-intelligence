---
phase: 11.1-pm-internal-link-deep-placement-text-filter-warning
plan: 02
subsystem: ui
tags: [react, vitest, health-panel, score-sidebar, tab-toggle, lucide-react]

# Dependency graph
requires:
  - phase: 11.1-plan-01
    provides: getHealthStatus, hasAnyWarning, HealthStatus interface from graph-utils.ts
provides:
  - HealthPanel component (3-metric badge list, summary line, warnings-first sort, toggle filter)
  - [Score|Health] tab toggle in ScoreSidebar
affects:
  - packages/web/src/components/ScoreSidebar.tsx (tab toggle added)
  - packages/web/src/components/HealthPanel.tsx (new component)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TDD red-green: failing tests committed before implementation for both HealthPanel and ScoreSidebar tabs"
    - "Span-wrapper pattern for lucide-react icons in tests: data-testid on <span> not on SVG (SVG className is SVGAnimatedString, not string)"
    - "Conditional fragment {activeTab === 'score' && <> ... </>} wraps existing Score content unchanged"

key-files:
  created:
    - packages/web/src/components/HealthPanel.tsx
    - packages/web/src/components/HealthPanel.test.tsx
  modified:
    - packages/web/src/components/ScoreSidebar.tsx
    - packages/web/src/components/ScoreSidebar.test.tsx

key-decisions:
  - "data-testid placed on <span> wrapper around lucide-react icons — SVG elements have SVGAnimatedString className, not string; toMatch() fails on objects"
  - "Tab underline style (border-b-2 border-blue-500 -mb-px) matches existing blue accent; fits sidebar header aesthetic without pill tabs"
  - "HealthPanel receives nodes/depthMap/outboundMap — same props ScoreSidebar already has; no new prop threading needed"
  - "Icons chosen: Link2 (Links), Layers (Depth — reusing Phase 8 icon), Tag (Tags) — all in lucide-react vocab, no new deps"
  - "Pre-existing test 'root node button has 12px base left padding' updated to query by URL text content instead of getAllByRole('button')[0] — tab buttons are now first in DOM order"

patterns-established:
  - "Icon badge pattern: <span data-testid='badge-X' className={condition ? WARN_CLASS : OK_CLASS}><Icon size={14} /></span>"
  - "Health row: flex row with flex-1 truncated URL + flex-shrink-0 badge group; no onClick (read-only)"
  - "Tab toggle: local useState<'score'|'health'> in ScoreSidebar; conditional fragments for each tab body"

requirements-completed: [HEALTH-04, HEALTH-05, HEALTH-06, HEALTH-07, HEALTH-08, HEALTH-10]

# Metrics
duration: 4min
completed: 2026-04-18
---

# Phase 11.1 Plan 02: PM Health Check Panel Summary

**`[Score|Health]` tab toggle in ScoreSidebar with new HealthPanel showing 3-metric badges (Links/Depth/Tags), warnings-first sort, summary line, and "Show warnings only" toggle — read-only, no canvas interaction**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-04-18T07:45:15Z
- **Completed:** 2026-04-18T07:48:54Z
- **Tasks:** 2 auto + 1 checkpoint (auto-approved via auto_advance)
- **Files modified:** 4

## Accomplishments

- Created `HealthPanel.tsx`: self-contained 3-metric health list component (80+ lines)
  - Summary line: "X / N pages have warnings"
  - Per-node rows with Link2 / Layers / Tag icon badges (warn=red-500, ok=muted-fg, na=hidden)
  - Warnings-first sort then alphabetical by urlTemplate
  - "Show warnings only" checkbox (local state, defaults off)
  - Empty state: "No nodes to check"
  - Read-only (no onClick, no fitView, no setNodes)
- Created `HealthPanel.test.tsx`: 14 passing Vitest tests covering all D-03 through D-11 behaviors
- Modified `ScoreSidebar.tsx`: added `[Score|Health]` tab toggle at sidebar top; Score content wrapped in conditional fragment; Health tab renders `<HealthPanel>`
- Modified `ScoreSidebar.test.tsx`: added 4 new tab-toggle tests; updated existing `'root node button has 12px base left padding'` test to query by text not array index (regression fix from tab DOM order change)

## Task Commits

1. **Task 1 RED — Write failing HealthPanel tests** - `489665c` (test)
2. **Task 1 GREEN — Implement HealthPanel component** - `3829c43` (feat)
3. **Task 2 RED — Write failing ScoreSidebar tab tests** - `f0116ec` (test)
4. **Task 2 GREEN — Wire tab toggle into ScoreSidebar** - `a0d5a5f` (feat)

## Files Created/Modified

- `packages/web/src/components/HealthPanel.tsx` — New component (118 lines): summary, rows, badges, sort, toggle
- `packages/web/src/components/HealthPanel.test.tsx` — 14 unit tests, no ReactFlowProvider needed
- `packages/web/src/components/ScoreSidebar.tsx` — Added tab bar, conditional Score/Health rendering, HealthPanel import
- `packages/web/src/components/ScoreSidebar.test.tsx` — Added 4 tab-toggle tests; fixed 1 existing test for new DOM order

## Decisions Made

- **Icon wrapping:** data-testid placed on `<span>` around lucide icons instead of directly on SVG — SVG `className` is `SVGAnimatedString` not a plain string, so Vitest's `.toMatch()` would fail
- **Tab styling:** underline style (`border-b-2 border-blue-500 -mb-px`) chosen — fits existing sidebar aesthetic better than pill tabs
- **Icon choices:** Link2 (Links), Layers (Depth — same as Phase 8), Tag (Tags) — all from existing lucide-react dep
- **Backward-compat:** existing test for `paddingLeft` updated to find button by URL text content — tab buttons were inserted before score-row buttons in DOM order

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] SVG className is SVGAnimatedString, not string**
- **Found during:** Task 1 GREEN (when running tests)
- **Issue:** `screen.getByTestId('badge-links').className` returned an SVGAnimatedString object; Vitest's `.toMatch()` expects a string
- **Fix:** Wrapped each lucide icon in a `<span data-testid="badge-X" className={...}>` — placed the testid and className on the HTML span wrapper, not the SVG element
- **Files modified:** `packages/web/src/components/HealthPanel.tsx`
- **Commit:** `3829c43` (fix applied in same GREEN commit)

**2. [Rule 1 - Bug] Tab DOM order broke existing padding test**
- **Found during:** Task 2 GREEN (full suite run)
- **Issue:** `screen.getAllByRole('button')[0]` previously returned the first score-row button; after adding 2 tab buttons, index 0 is now the Score tab button with no paddingLeft style
- **Fix:** Updated query to find button by URL text content: `screen.getAllByRole('button').find((b) => b.textContent?.includes('/about'))`
- **Files modified:** `packages/web/src/components/ScoreSidebar.test.tsx`
- **Commit:** `a0d5a5f` (fix included in Task 2 commit)

## Test Results

- `HealthPanel.test.tsx`: 14/14 passed
- `ScoreSidebar.test.tsx`: 42/42 passed (38 existing + 4 new)
- Full suite: 309/313 passed (4 pre-existing EditPopover failures from quick task 260418-gy5 — out of scope)

## Pre-existing Issues (Out of Scope)

- `EditPopover.test.tsx`: 4 failing tests from quick task 260418-gy5 (cluster tag chip UX changes). Not introduced by this plan.
- TypeScript build errors in App.tsx, ScenarioTabBar.test.tsx, UrlNode.test.tsx, useScenarios.test.ts, graph-utils.test.ts — all pre-existing, no errors in HealthPanel.tsx or ScoreSidebar.tsx.

## Known Stubs

None — all 3 metrics (Links, Depth, Tags) are wired to real data via getHealthStatus/depthMap/outboundMap props. No placeholder values or TODO markers.

## Checkpoint Notes

Task 3 (human-verify) auto-approved via `auto_advance: true` workflow config. Automated verification: HealthPanel (14 tests) and ScoreSidebar (42 tests) both green at approval time.

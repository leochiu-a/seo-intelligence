---
phase: 999.5-topical-cluster-tags
plan: 04
subsystem: ui
tags: [react, tailwind, cluster-colors, urlnode, canvas]

# Dependency graph
requires:
  - phase: 999.5-02
    provides: cluster-colors.ts with getClusterColor + CLUSTER_PALETTE
  - phase: 999.5-03
    provides: data.tags wired through handleSave / data.onUpdate into UrlNodeData

provides:
  - Left-edge 4px stripe DIV on UrlNode card, split into 1-3 equal vertical bands by tag
  - Tag chip spans in subtitle row with color dot prefix per tag
  - +N overflow indicator for tags.length > 3
  - TODO Plan 07 comment for stripe-under-dim persistence deferral

affects: [999.5-07, 999.5-08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "flex flex-col + flex-1 for equal-height stripe bands regardless of count"
    - "data-testid selectors for cluster-stripe, cluster-chip, cluster-overflow"
    - "visibleBandTags = tags.slice(0,3) + overflowCount = Math.max(0, tags.length-3)"

key-files:
  created: []
  modified:
    - packages/web/src/components/UrlNode.tsx
    - packages/web/src/components/UrlNode.test.tsx

key-decisions:
  - "Stripe div uses absolute left-0 top-0 bottom-0 w-1 flex flex-col overflow-hidden rounded-l-xl pointer-events-none — anchors to existing relative outer div"
  - "Dim-persistence deferred to Plan 07 via TODO comment; Plan 04 renders stripe normally under current node.style.opacity inheritance"
  - "Chips render AT END of subtitle row after over-linked indicator, separated by · separator before first chip"

patterns-established:
  - "Stripe positioning: absolute left-0 top-0 bottom-0 w-1 flex flex-col overflow-hidden rounded-l-xl pointer-events-none"
  - "Subtitle order: pageCount → weak → orphan → unreachable → depth → over-linked → cluster chips"

requirements-completed: [CLUSTER-08, CLUSTER-11]

# Metrics
duration: 5min
completed: 2026-04-17
---

# Phase 999.5 Plan 04: UrlNode Left-Edge Stripe + Subtitle Tag Chips Summary

**4px left-edge stripe with 1-3 equal vertical color bands and subtitle tag chips wired to getClusterColor, making cluster membership immediately scannable on the UrlNode card**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-17T14:56:00Z
- **Completed:** 2026-04-17T15:01:20Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- UrlNode card renders a 4px left-edge stripe split into equal bands (1=full, 2=halves, 3=thirds) when `data.tags` is non-empty
- Tag chips appear in the subtitle row with a color dot prefix, after the over-linked indicator
- Overflow indicator `+N` renders when `tags.length > 3` (capped at 3 stripe bands)
- TODO Plan 07 comment placed in UrlNode.tsx for the stripe-under-dim persistence deferral
- 7 new cluster rendering tests pass alongside all existing 7 UrlNode tests (14 total pass)

## Task Commits

1. **Task 1: Add stripe + chips rendering to UrlNode.tsx with tests** - `0d660c1` (feat)

## Files Created/Modified
- `packages/web/src/components/UrlNode.tsx` - Import getClusterColor, add tags derivation, stripe DIV as first card child, tag chips at end of subtitle row
- `packages/web/src/components/UrlNode.test.tsx` - 7 new cluster stripe/chip tests using data-testid selectors

## Decisions Made

- **Stripe className:** `absolute left-0 top-0 bottom-0 w-1 flex flex-col overflow-hidden rounded-l-xl pointer-events-none` — anchors to the outer card's existing `relative` positioning. `flex flex-col` + `flex-1` per band gives equal height regardless of count.
- **Subtitle order (as rendered):** `pageCount → weak → orphan → unreachable → depth → over-linked → cluster chips (NEW)` — chips go last per plan spec, preserving Phase 8/10 ordering.
- **Dim-persistence deferred to Plan 07:** Stripe currently inherits `node.style.opacity` from React Flow's wrapper div. Plan 07 will handle the dim-mechanism change. A `TODO Plan 07` comment is in place.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 04 complete: UrlNode card now visually reflects cluster assignment with stripe + chips
- Plan 05 (sidebar cluster panel) and Plan 06 (edge coloring) can proceed in parallel
- Plan 07 will resolve the stripe-under-dim persistence concern noted in the TODO comment

---
*Phase: 999.5-topical-cluster-tags*
*Completed: 2026-04-17*

## Self-Check: PASSED

- [x] `packages/web/src/components/UrlNode.tsx` — exists and modified
- [x] `packages/web/src/components/UrlNode.test.tsx` — exists and modified
- [x] Commit `0d660c1` — exists in git log

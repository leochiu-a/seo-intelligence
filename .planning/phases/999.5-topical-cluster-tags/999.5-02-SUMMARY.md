---
phase: 999.5-topical-cluster-tags
plan: 02
subsystem: ui
tags: [vitest, tailwind, typescript, cluster-colors, djb2, hash]

# Dependency graph
requires: []
provides:
  - CLUSTER_PALETTE: readonly array of 8 ClusterColor entries (teal, cyan, sky, pink, rose, orange, lime, fuchsia)
  - getClusterColor(tag): deterministic DJB2 hash → palette lookup returning { stripe, dot, edge }
  - ClusterColor interface with stripe (Tailwind class), dot (Tailwind class), edge (hex string)
  - Snapshot-locked palette ensuring reorders require explicit test approval
affects:
  - 999.5-03 (UrlNode stripe + chips — imports getClusterColor)
  - 999.5-04 (ScoreSidebar dots — imports getClusterColor)
  - 999.5-05 (LinkCountEdge coloring — imports getClusterColor)
  - 999.5-06 (FilterPanel cluster section — imports CLUSTER_PALETTE)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - DJB2 hash for deterministic string → palette index mapping (zero deps, pure function)
    - Object.freeze for readonly palette tuple at runtime
    - Vitest toMatchSnapshot for palette stability regression testing

key-files:
  created:
    - packages/web/src/lib/cluster-colors.ts
    - packages/web/src/lib/cluster-colors.test.ts
    - packages/web/src/lib/__snapshots__/cluster-colors.test.ts.snap
  modified: []

key-decisions:
  - "DJB2 chosen for hash: zero dependencies, deterministic, no Math.random/Date, snapshot-testable"
  - "8-entry palette uses teal/cyan/sky/pink/rose/orange/lime/fuchsia — avoids green/amber/red/indigo/blue/violet reserved by existing UI tiers"
  - "Object.freeze at declaration ensures runtime immutability alongside TypeScript readonly modifier"
  - "edge field uses hex strings (not Tailwind classes) because React Flow inline styles do not accept Tailwind class names"
  - "Empty string is a valid input — DJB2('') = 5381 → index 5381 % 8 = 5 → orange-400"

patterns-established:
  - "Snapshot test locking palette order: any palette reorder breaks the snapshot test loudly, requiring explicit approval"
  - "Separate hex constants for edge strokes alongside Tailwind class strings for standard DOM styling"

requirements-completed:
  - CLUSTER-07

# Metrics
duration: 5min
completed: 2026-04-17
---

# Phase 999.5 Plan 02: Cluster Color Palette Summary

**DJB2-hashed 8-entry color palette (teal→fuchsia) with frozen CLUSTER_PALETTE array and getClusterColor() deterministic lookup, snapshot-tested to lock palette stability**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-17T09:17:00Z
- **Completed:** 2026-04-17T09:18:30Z
- **Tasks:** 2 (TDD: RED → GREEN)
- **Files modified:** 3 created

## Accomplishments

- Created standalone `cluster-colors.ts` module with zero external dependencies
- Implemented DJB2 hash for pure, deterministic tag → palette index mapping
- 8-entry palette (teal, cyan, sky, pink, rose, orange, lime, fuchsia) avoids all existing UI color semantics
- 7 vitest tests all pass, including 2 snapshot tests locking palette + canonical tag mappings
- TypeScript compiles cleanly (`tsc --noEmit` exits 0)

## Final Palette Entries

| Index | stripe | dot | edge (hex) |
|-------|--------|-----|------------|
| 0 | bg-teal-400 | bg-teal-400 | #2dd4bf |
| 1 | bg-cyan-400 | bg-cyan-400 | #22d3ee |
| 2 | bg-sky-400 | bg-sky-400 | #38bdf8 |
| 3 | bg-pink-400 | bg-pink-400 | #f472b6 |
| 4 | bg-rose-400 | bg-rose-400 | #fb7185 |
| 5 | bg-orange-400 | bg-orange-400 | #fb923c |
| 6 | bg-lime-400 | bg-lime-400 | #a3e635 |
| 7 | bg-fuchsia-400 | bg-fuchsia-400 | #e879f9 |

## Canonical Tag Mappings (from snapshot)

- `getClusterColor('food')` → index = DJB2('food') % 8 = locked in snapshot
- `getClusterColor('hotel')` → locked in snapshot
- `getClusterColor('taipei')` → locked in snapshot
- `getClusterColor('')` → index 5381 % 8 = 5 → orange-400 (graceful fallback)

## Task Commits

TDD RED → GREEN pattern with 2 atomic commits:

1. **Task 1: RED — Add failing cluster-colors.test.ts** - `4b28469` (test)
2. **Task 2: GREEN — Implement cluster-colors.ts** - `1545631` (feat)

## Files Created/Modified

- `packages/web/src/lib/cluster-colors.ts` — CLUSTER_PALETTE, getClusterColor, ClusterColor interface, djb2 private hash
- `packages/web/src/lib/cluster-colors.test.ts` — 7 vitest tests covering palette constraints, determinism, snapshots, edge cases
- `packages/web/src/lib/__snapshots__/cluster-colors.test.ts.snap` — Snapshot file locking palette order and canonical tag color mappings

## Decisions Made

- DJB2 chosen: zero deps, pure function, hash * 33 + c — deterministic across all JS runtimes
- Object.freeze used alongside TypeScript `readonly` for runtime + compile-time immutability
- edge hex strings are hardcoded constants matching Tailwind 400 shades exactly
- Empty string input valid by design — hash of empty string = 5381 (DJB2 seed), maps to palette[5381 % 8]

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `node_modules` was missing in the worktree on first run — installed via `pnpm install` at repo root. Tests ran successfully after install. (Rule 3 auto-fix — blocking issue resolved.)

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `cluster-colors.ts` is a standalone module with stable exports — downstream plans (Plans 03-06) can import `CLUSTER_PALETTE`, `getClusterColor`, and `ClusterColor` immediately
- Snapshot file committed ensures any palette reorder in future PRs will fail the test loudly
- No blockers

---
*Phase: 999.5-topical-cluster-tags*
*Completed: 2026-04-17*

---
status: complete
phase: 999.5-topical-cluster-tags
source:
  - 999.5-01-SUMMARY.md
  - 999.5-02-SUMMARY.md
  - 999.5-03-SUMMARY.md
  - 999.5-04-SUMMARY.md
  - 999.5-05-SUMMARY.md
  - 999.5-06-SUMMARY.md
  - 999.5-07-SUMMARY.md
started: 2026-04-17T00:00:00Z
updated: 2026-04-17T00:00:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running dev server. Start it fresh with `pnpm dev` (or equivalent). The app boots without errors, loads in the browser, and the canvas with URL nodes appears without console errors.
result: pass

### 2. Add Tags to a Node via EditPopover
expected: Double-click a URL node to open the edit popover. A "Cluster Tags" section appears at the bottom of the popover. Type a tag name (e.g. "food") into the tag input and press Enter. A chip appears showing the tag name with a small colored dot. Type another tag (e.g. "taipei") and press Enter — a second chip appears. Click Confirm. The node is saved with those tags.
result: pass

### 3. Left-Edge Stripe Appears on Tagged Nodes
expected: After saving a node with tags, the URL node card shows a 4px colored stripe on its left edge. The stripe color matches the tag's cluster color. If the node has 2 tags, the stripe splits into 2 equal vertical bands, each a different color. If the node has 3+ tags, up to 3 bands appear.
result: pass

### 4. Tag Chips in Node Subtitle Row
expected: The URL node card subtitle row shows small tag chip labels (with a colored dot prefix) for each tag. If there are more than 3 tags, an overflow indicator "+N" appears instead of listing all chips.
result: pass

### 5. Tag Autocomplete Suggestions
expected: Open the edit popover for a second node. In the Cluster Tags input, start typing — suggestions from other nodes' existing tags appear as a dropdown. Selecting a suggestion from the dropdown fills the input. Pressing Enter adds it as a chip. No suggestions appear when no other node has tags.
result: issue
reported: "沒有出現 dropdown"
severity: major

### 6. Same-Cluster Edges Show Colored Stroke
expected: Create two nodes and give them the same tag (e.g. "food"). Add a link edge between them. The edge stroke should appear in a muted color (not gray #9CA3AF) matching the shared cluster's color. Edges between untagged nodes or nodes with different tags remain gray.
result: issue
reported: "沒有出現 muted color"
severity: major

### 7. Cluster Dots in Score Sidebar
expected: Open the Score Sidebar (right panel). For nodes that have tags, small colored dots appear before the URL template text in the ranked list. The dot colors match the node's cluster colors. Nodes with no tags show no dots.
result: pass

### 8. By Cluster Filter Section in Filter Panel
expected: Open the filter/highlight panel. A "By cluster" section appears below the "By placement" section. Each unique tag across all nodes appears as a selectable filter option with its cluster color. When no node has tags, the "By cluster" section is hidden entirely.
result: pass

### 9. Cluster Filter Highlights Matching Nodes
expected: In the filter panel, click a cluster tag filter (e.g. "food"). Nodes with that tag remain fully visible. Nodes without that tag have their card content dimmed (low opacity). The left-edge stripe on dimmed nodes stays vivid/fully opaque — only the content area dims, not the stripe.
result: pass

### 10. Placement x Cluster AND-Combine Filter
expected: Select a placement filter AND a cluster filter simultaneously. Only nodes that match BOTH the selected placement AND the selected cluster tag remain fully highlighted. Nodes that match only one filter are dimmed. (Within each dimension, selecting multiple filters expands the set — OR logic. Across dimensions, the intersection is used — AND logic.)
result: pass

## Summary

total: 10
passed: 8
issues: 2
pending: 0
skipped: 0

## Gaps

- truth: "Cluster Tags autocomplete dropdown appears when typing in the tag input of a node that has suggestions from other nodes"
  status: failed
  reason: "User reported: 沒有出現 dropdown"
  severity: major
  test: 5
  artifacts: []
  missing: []

- truth: "Edges between two nodes sharing the same tag render with a colored stroke matching the cluster color (not gray)"
  status: failed
  reason: "User reported: 沒有出現 muted color — edges between /blog (food, taipei) and /blog/seo (food) remain gray"
  severity: major
  test: 6
  artifacts: []
  missing: []

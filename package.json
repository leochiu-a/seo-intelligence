{
  "name": "seo-intelligence",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.18.1",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "scripts": {
    "dev": "pnpm --filter @seo-intelligence/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "preview": "pnpm --filter @seo-intelligence/web preview",
    "type-check": "pnpm -r type-check"
  }
}
